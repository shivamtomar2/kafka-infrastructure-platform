---
- name: Configure Kafka Cluster
  hosts: role_kafka

  become: true
  gather_facts: false

  any_errors_fatal: true

  pre_tasks:
    - name: Gather required facts
      setup:

  roles:
    - role: java
      tags:
        - java

    - role: kafka
      tags:
        - kafka
